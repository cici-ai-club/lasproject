savedSearch Explain:
"dup": has all the saved searches include duplicate ones
"searches": all the saved searches, real data, with the search content and the justification words
"hypos": which container has which item
validuserlog explain:
The "assign" could assign document or searches
